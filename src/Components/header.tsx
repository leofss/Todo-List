import React from 'react';

import styles from "./header.module.css"

const Header = () => {
    return(
        <header className={styles.header}>
            <h1> Todo-List com React + TS</h1> 
       </header>
    )
}

export default Header   